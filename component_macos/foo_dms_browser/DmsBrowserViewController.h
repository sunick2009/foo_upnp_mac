#pragma once

#import <Cocoa/Cocoa.h>

// The browser tree UI (ADR-014). Reusable: hosted by the standalone
// window in M3, planned to back a ui_element_mac panel in M4.
@interface DmsBrowserViewController : NSViewController
@end
